package e2;

public class Winner {
    public String selectCandidates (TVRealityList tvRealityList){
        while(tvRealityList.ListSize()!=1) tvRealityList.iterator();
        return tvRealityList.iterator().toString();
    }
}
